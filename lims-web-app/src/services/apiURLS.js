import { environment } from "../environments/environment.local";

const localURLS = {
    AUTH: environment.AUTH,
    MASTERS: environment.MASTERS,
    LAB_MANAGEMENT: environment.LAB_MANAGEMENT
}

export const Urls = localURLS